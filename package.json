{
  "name": "outpost-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Embeddable durable execution library (MySQL-only, correctness-first).",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test",
    "typecheck": "yarn workspaces run typecheck",
    "clean": "yarn workspaces run clean"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
